import React from "react";
import { RiPhoneLine } from "react-icons/ri";

const Phone = ({ size = 16, className = "" }) => (
  <RiPhoneLine size={size} className={className} />
);

export default Phone;
